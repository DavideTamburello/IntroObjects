using System;
using ConfrontoOggettiDemo;

namespace I;

public class test
{
    /*
    public bool Equals(Box? other)
    {
        if(other is null)
        {
            return false
        }
        return larghezza == larghezza.other && ...
    }

    public override bool Equals(object? obj)
    {
        if(obj is null)
        {
            return false
        }
        if(obj is Box other)
        {
            return Equals(other)
        }
        return false
    }

    */   
}